{
  "name": "libcobblersignatures-commit-tooling",
  "private": true,
  "description": "Commit message linting tooling for libcobblersignatures (commitlint) - not a published package",
  "devDependencies": {
    "@commitlint/cli": "^21.2.1",
    "@commitlint/config-conventional": "^21.2.0"
  }
}
